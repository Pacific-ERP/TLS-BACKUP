<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- form -->
        <record id="pe_customer_stock_wizard_form" model="ir.ui.view">
            <field name="name">Pacific-ERP pe_customer_stock : customer.stock.wizard(form)</field>
            <field name="model">customer.stock.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <group>
                                <field name="direct_delivery"/>
                                <field name="partner_id" required="1"/>
                                <field name="origin"/>
                                <field name="picking_id" invisible="0"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Opérations">
                                <field name="wizard_lines" mode="tree">
                                    <tree create="0" delete="0" editable="top" edit="1">
                                        <field name="product_id" readonly="1"/>
                                        <field name="destination_id" 
                                            attrs="{'required': [('direct_delivery','=', True)], 'column_invisible' : [('parent.direct_delivery','=', False)]}" 
                                            context="{'default_location_id': 5}"/>
                                        <field name="quantity" readonly="1"/>
                                        <field name="product_uom_id" readonly="1"/>
                                        <!-- Invisible -->
                                        <field name="direct_delivery"  readonly="1" optional="hide"/>
                                        <field name="product_uom_category_id" readonly="1" optional="hide"/>
                                        <field name="wizard_id" readonly="1" optional="hide"/>
                                        <field name="move_id" readonly="1" optional="hide"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                    <footer>
                        <button string="Confirmer et voir" name="button_confirm" context="{'view_customer_picking':True}" type="object" class="btn-primary"/>
                        <button string="Confirmer" name="button_confirm" type="object" class="btn-primary"/>
                        <button string="Annuler" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
       
    </data>
</odoo>